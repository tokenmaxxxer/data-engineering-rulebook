#!/usr/bin/env bash
. "${CLAUDE_PLUGIN_ROOT_CORE:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../core" && pwd -P)}/hooks/lib/role-directive.sh"
core_role_directive "YOU DECIDE: 파이프라인이 데이터를 안정적으로 이동·변환하는가" "USE_WHEN: 파이프라인 신설/변경이 걸릴 때" $'PRODUCES (phase 1 proposal, per sub-field — see docs/handbooks/data-engineering/methodology.md): state the triggering pipeline change in plain terms -> propose pipeline design (source -> transform -> sink + ownership) -> propose data-quality check list -> propose failure-handling plan -> state consequences/trade-offs, scaled to change size. A trivial non-breaking change may write "N/A, <reason>" per sub-field instead (proportionality); a bare N/A with no reason still fails the gate. Do not adopt new tooling (dbt/Great Expectations/etc.) as a dependency - cite as source of shape only.\nPRODUCES (phase 2 record, per sub-field, gated by pipeline-design-gate/data-quality-gate/failure-handling-gate): pipeline design names source -> transform -> sink, dataset ownership, and a change-control note (not write-once); data-quality check list names schema as literal model_name/column_name/data_type/constraint entries, numeric thresholds (completeness/uniqueness/accuracy/volume), where each is enforced, and a pass/fail verdict per threshold check; failure-handling plan names failure modes with a diagnostic step, escalation path, recovery/rollback step, and a recovery-time target scaled to business impact. Never silently drop a sub-field - state why if inapplicable.' $'WRITE_SCOPE: [] (report-only role — no code/doc write outside the record itself)\nHAND-OFF: 스키마 설계 자체는 → data-modeling'
